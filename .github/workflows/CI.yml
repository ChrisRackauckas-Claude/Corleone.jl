name: CI
on:
  pull_request:
    branches:
      - main
    paths-ignore:
      - 'docs/**'
  push:
    branches:
      - main
    paths-ignore:
      - 'docs/**'
concurrency:
  group: ${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: ${{ github.ref_name != github.event.repository.default_branch || github.ref != 'refs/tags/v*' }}
jobs:
  tests:
    name: "${{ matrix.group }}"
    strategy:
      fail-fast: false
      matrix:
        group:
          - Core
          - QA
        version:
          - 'lts'
          - '1.11'
          - '1'
          - 'pre'
        exclude:
          # QA (Aqua) only needs the current release.
          - group: QA
            version: 'lts'
          - group: QA
            version: '1.11'
          - group: QA
            version: 'pre'
    uses: "SciML/.github/.github/workflows/tests.yml@v1"
    with:
      julia-version: "${{ matrix.version }}"
      group: "${{ matrix.group }}"
      group-env-name: "GROUP"
      check-bounds: "auto"
      coverage-directories: "src"
    secrets: "inherit"
